# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.caching=true
org.gradle.parallel=true

# Build mode: true = Legacy Fabric only (default), false = include upstream modern subprojects
legacy.mode=true

# Active Minecraft version profile (see versions/ for per-version property files)
versionProfile=1.8.9

# Mod metadata
mod_version=0.1.0-SNAPSHOT
maven_group=io.github.maxjubayeryt
archives_base_name=legacy-sodium
